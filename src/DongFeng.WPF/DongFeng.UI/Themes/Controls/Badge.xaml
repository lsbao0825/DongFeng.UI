<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:DongFeng.UI.Controls">

    <Style x:Key="DFBadge" TargetType="{x:Type controls:Badge}">
        <Setter Property="BadgeBackground" Value="{DynamicResource Brush_Error}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Badge}">
                    <Grid>
                        <ContentPresenter Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        
                        <Border x:Name="BadgeBorder"
                                Background="{TemplateBinding BadgeBackground}"
                                BorderBrush="{DynamicResource Brush_Surface}"
                                BorderThickness="1"
                                CornerRadius="10"
                                MinWidth="20"
                                Height="20"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top">
                            <Border.RenderTransform>
                                <TranslateTransform X="{Binding Offset.X, RelativeSource={RelativeSource TemplatedParent}}"
                                                    Y="{Binding Offset.Y, RelativeSource={RelativeSource TemplatedParent}}" />
                            </Border.RenderTransform>
                            <!-- Default Margin for standard overlap -->
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Margin" Value="-10,-10,0,0" />
                                </Style>
                            </Border.Style>
                            
                            <TextBlock x:Name="BadgeText" 
                                       Text="{TemplateBinding BadgeText}"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontSize="{TemplateBinding FontSize}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Padding="6,0" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Dot Mode -->
                        <Trigger Property="IsDot" Value="True">
                            <Setter TargetName="BadgeText" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="BadgeBorder" Property="MinWidth" Value="8" />
                            <Setter TargetName="BadgeBorder" Property="Height" Value="8" />
                            <Setter TargetName="BadgeBorder" Property="CornerRadius" Value="4" />
                            <Setter TargetName="BadgeBorder" Property="Padding" Value="0" />
                            <Setter TargetName="BadgeBorder" Property="Margin" Value="-4,-4,0,0" />
                        </Trigger>
                        
                        <!-- Hide if empty text (and not dot) -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="BadgeText" Value="" />
                                <Condition Property="IsDot" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BadgeBorder" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>
                        
                        <!-- Hide if null text (and not dot) -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="BadgeText" Value="{x:Null}" />
                                <Condition Property="IsDot" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BadgeBorder" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DFBadge}" TargetType="{x:Type controls:Badge}" />

</ResourceDictionary>
