<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:DongFeng.UI.Controls"
    xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/DongFeng.UI;component/Themes/BaseStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:DFEmpty}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Secondary}"/>
        <Setter Property="Template">
             <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:DFEmpty}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <!-- Image Area -->
                        <Grid HorizontalAlignment="Center" Margin="0,0,0,16">
                            <!-- Default Icon -->
                            <icon:PackIconMaterial Kind="InboxOutline" 
                                                   Width="{TemplateBinding ImageWidth}" 
                                                   Height="{TemplateBinding ImageHeight}"
                                                   Foreground="{DynamicResource Brush_Border}"
                                                   x:Name="DefaultIcon"
                                                   Visibility="Collapsed"/>
                            
                            <!-- ImageSource -->
                            <Image Source="{TemplateBinding ImageSource}"
                                   Width="{TemplateBinding ImageWidth}"
                                   Height="{TemplateBinding ImageHeight}"
                                   Stretch="Uniform"
                                   x:Name="CustomImage"/>
                        </Grid>

                        <!-- Description -->
                        <ContentPresenter ContentSource="Description"
                                          TextBlock.Foreground="{TemplateBinding Foreground}"
                                          TextBlock.FontSize="14"
                                          HorizontalAlignment="Center"
                                          Margin="0,0,0,16"/>

                        <!-- Action Content -->
                        <ContentPresenter HorizontalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <!-- If ImageSource is null, show Default Icon and hide Image -->
                        <Trigger Property="ImageSource" Value="{x:Null}">
                            <Setter TargetName="DefaultIcon" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="CustomImage" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
             </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

