<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:DongFeng.UI.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/DongFeng.UI;component/Themes/BaseStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:DFToggleSwitch}">
        <Setter Property="Background" Value="{DynamicResource DF_Control_Fill_Brush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DF_Toggle_Border_Unchecked}" />
        <Setter Property="Foreground" Value="{DynamicResource DF_Text_Primary_Brush}" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:DFToggleSwitch}">
                    <Grid Background="Transparent" SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!--  Content  -->
                        <ContentPresenter
                            x:Name="contentPresenter"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Content}"
                            Focusable="False"
                            RecognizesAccessKey="True" />

                        <!--  Switch Body  -->
                        <Grid
                            Grid.Column="1"
                            Width="40"
                            Height="22">
                            <!--  Track  -->
                            <Border
                                x:Name="Track"
                                Background="{DynamicResource DF_Toggle_Border_Unchecked}"
                                CornerRadius="11">
                                <Border.Effect>
                                    <DropShadowEffect
                                        BlurRadius="1"
                                        Opacity="0.1"
                                        ShadowDepth="1" />
                                </Border.Effect>
                            </Border>

                            <!--  Thumb  -->
                            <Border
                                x:Name="Thumb"
                                Width="18"
                                Height="18"
                                Margin="2,0,0,0"
                                HorizontalAlignment="Left"
                                Background="#FFFFFF"
                                CornerRadius="9">
                                <Border.Effect>
                                    <DropShadowEffect
                                        BlurRadius="2"
                                        Opacity="0.2"
                                        ShadowDepth="1" />
                                </Border.Effect>
                                <Border.RenderTransform>
                                    <TranslateTransform x:Name="ThumbTranslate" X="0" />
                                </Border.RenderTransform>
                            </Border>
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!--  Checked State  -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Track" Property="Background" Value="{DynamicResource DF_Toggle_Background_Checked}" />
                            <Setter TargetName="Thumb" Property="RenderTransform">
                                <Setter.Value>
                                    <TranslateTransform X="18" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <!--  Disabled State  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

