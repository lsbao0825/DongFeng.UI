<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:DongFeng.UI.Controls"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/DongFeng.UI;component/Themes/BaseStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:DFLoading}">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:DFLoading}">
                    <Grid>
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}" />

                        <Grid x:Name="PART_DFLoadingOverlay" Visibility="Collapsed">
                            <Border Background="{DynamicResource Brush_Surface}" Opacity="0.7" />


                            <StackPanel
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                                    <Path
                                        x:Name="Spinner"
                                        Width="32"
                                        Height="32"
                                        Data="M16,2 A14,14 0 0,0 2,16"
                                        RenderTransformOrigin="0.5,0.5"
                                        Stroke="{DynamicResource Brush_Primary}"
                                        StrokeThickness="3">
                                        <Path.RenderTransform>
                                            <RotateTransform x:Name="SpinnerTransform" Angle="0" />
                                        </Path.RenderTransform>
                                    </Path>
                                    <TextBlock
                                        x:Name="TxtDFLoading"
                                        Margin="0,10,0,0"
                                        HorizontalAlignment="Center"
                                        Foreground="{DynamicResource Brush_Text_Primary}"
                                        Text="{TemplateBinding DFLoadingText}" />
                                </StackPanel>
                                <Button
                                    x:Name="PART_CancelButton"
                                    Width="32"
                                    Height="32"
                                    Margin="20,0,0,0"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Visibility="Collapsed">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}">
                                                            <Path
                                                                Width="12"
                                                                Height="12"
                                                                Data="M1,1 L11,11 M1,11 L11,1"
                                                                Stretch="Uniform"
                                                                Stroke="{TemplateBinding Foreground}"
                                                                StrokeThickness="2" />
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Secondary}" />
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource Brush_Primary}" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                            </StackPanel>

                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="Loaded" SourceName="Spinner">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        RepeatBehavior="Forever"
                                        Storyboard.TargetName="SpinnerTransform"
                                        Storyboard.TargetProperty="Angle"
                                        From="0"
                                        To="360"
                                        Duration="0:0:1" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <Trigger Property="IsDFLoading" Value="True">
                            <Setter TargetName="PART_DFLoadingOverlay" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="DFLoadingText" Value="">
                            <Setter TargetName="TxtDFLoading" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="DFLoadingText" Value="{x:Null}">
                            <Setter TargetName="TxtDFLoading" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsCancellable" Value="True">
                            <Setter TargetName="PART_CancelButton" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
